# 0.8.1 代码调试记录

以下是 0.8.1 历史记录；0.9.0 已改为自动 Agent 流程，当前行为以 [AGENT-NOTES.md](AGENT-NOTES.md) 和 README 为准。

本次按要求只进行代码审查、Node.js 测试和模拟 DOM 验证；未操作招聘网站表单，未调用真实千问服务，未改动简历主库、方向数据、答案记忆或密钥配置。

## 已修复

| 问题 | 修复后的行为 |
| --- | --- |
| 扫描生成方案时就点击添加，预览尚未确认就修改网页 | 扫描只读取；添加作为单独方案项，用户确认后执行，新字段出现后再次预览确认 |
| 添加后固定等待 200ms，再次点击可能重复创建记录 | 等待字段数量增长；无增长就停止，保留明确错误 |
| 含“添加”的默认 submit 按钮、跳转链接也可能被点击 | 排除表单提交按钮、禁用按钮及真实跳转链接 |
| 日期控件派发 Enter，可能触发页面的提交快捷键 | 移除 Enter；原生 date/month 直接写入，其他不支持的控件报错 |
| 隐藏模板、禁用 fieldset、验证码等混入字段清单 | 按祖先可见性、禁用状态及验证码语义过滤；服务端也排除验证码等控件 |
| 包装式 label 不识别；readonly 自定义下拉框被当成只读字段；纯 combobox 漏识别 | 增加对应识别；原生 select 读取选项文本；radio 按所属 form 隔离 |
| name 属性里的结构化字段路径被漏掉；“学位”“学习形式”缺少映射 | 补充 name 路径及语义规则；包含输入框的容器不再充当标题 |
| 短值只要包含于完整值就算“已填正确” | 改为完整值比较，仅保留月份到首日的明确日期兼容 |
| 缺少学校/公司名但已有其他内容的记录被当空行覆盖 | 有内容的记录保留，不再作为空白条目复用 |
| 记录序号有间隔时，虚构不存在的空行；模糊名称误匹配学校或项目 | 只复用真实存在的空行；身份使用完整名称或主库别名匹配 |
| 作品链接分支位于不存在的记录映射检查之后，永远走不到 | 作品链接直接映射到主库作品集 |
| 原字段消失后，按区块和序号分数定位到其他字段 | 必须保留字段语义；无法唯一定位就报错，不猜测替代控件 |
| 预览后用户已经手改内容仍被覆盖 | 对比预览时的原值，变化后停止写入该项 |
| 框架截断内容仍被报告成功；编辑候选文案可以绕过 maxlength | 检查完整写入结果，并在写入前验证长度 |
| 主库内容超长时截短后仍默认勾选 | 标记需确认并解释截短原因 |
| 千问失败导致精确匹配结果也一起丢失 | 保留精确匹配；未知字段留空待确认，并提示降级 |
| 千问低置信度映射默认勾选；重复 ID、跨记录映射未校验 | AI 映射全部需确认；忽略重复结果，拒绝错记录的路径 |
| 切换方向或标签页后，旧请求返回重新展示旧方案 | 绑定方向、页面和修订号；立即失效并丢弃迟到响应 |
| 上一方向单字段草稿残留；重复点执行；重新渲染覆盖勾选状态 | 清空旧草稿、防止并发执行、保留用户勾选决策 |
| 页面脚本返回 ok:false 没有被当错误处理，导致虚报成功 | 统一检查响应并展示字段级失败原因 |

字段列表现在展示区块、记录序号、必填/选填、只读状态、控件类型、最大长度和当前值。

## 验证

- `npm run check`：通过。
- `npm test`：50 项通过，0 项失败；原有 16 项加 34 项回归测试。
- DOM 测试使用 jsdom，Chrome 消息与千问响应使用模拟数据，不需要浏览器或 API Key。
- ZIP 只包含 extension 目录；更新后需要重启本地服务、重新加载扩展并刷新招聘页。

## 仍需真实页面验证的边界

- React/Vue 控件的真实事件处理、浏览器网站权限、日期弹层与级联下拉框，需要后续在目标网站联调；本次没有验证这些网站当前版本的兼容性。
- iframe、Shadow DOM 内部表单尚未扫描；隐藏/折叠的记录需要先展开。
- 某些添加按钮会先打开编辑弹窗或要求手动保存记录。若未检测到新增字段，插件停止并提示，不会自动保存。
- 扩展不会点击保存、下一步或提交。网站本身的输入自动保存不受扩展控制，因此界面只声明“扩展未点击保存或提交”，不宣称网站一定未保存。
- 科技销售继续保持待补充状态，不能生成填表计划，也不会退回其他方向。
