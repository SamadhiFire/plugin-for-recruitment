# 0.9.7 自动填表 Agent

0.9.7：修复滴滴样式表单把“工作经验”等字段标签当成分区标题、把下拉框当前值当成字段名的问题；识别同一条经历中的起止日期与拆开的年/月控件，映射到对应简历记录并回读。拆分年月需要同步更新本地服务，旧服务会明确提示而不继续生成错误方案。通过合成 DOM 覆盖三条实习、三条项目及年/月填写；Chrome 调试连接仍缺少浏览器插件运行文件，未完成真实滴滴页面验证。

0.9.6：修复项目简历日期带日精度时跳过字节月份控件的问题。字节月份控件、原生 month、Element 月份控件按年月填写与回读校验；按日控件保留日精度。仅扩展更新，无需重启本地服务；旧页面需重新加载内容脚本，刷新前请处理未保存内容。

0.9.5：分析状态恢复实际步骤（读取页面 → 检查并补齐经历 → 匹配内容与复核）与总耗时，不按时间推测进度；继续兼容 0.9.3 本地服务。

目标：内部完成识别、复核和纠错，用户只在最终方案确认并对剩余问题做轻量增删补查。优先避免缺漏、错填；允许新增可能重复的明确经历，保留旧条目供用户最后删除。不会自动保存、提交申请或删除网页记录。

## 操作流程

选择方向 → 自动补齐空白记录并扫描 → 内部分析并准备填写 → 最终确认一次 → 自动填写 → 回读检查 → 展示剩余问题。

- 已通过内部检查的内容默认勾选并收起，仍可展开查看、修改或取消。
- 不能确定的字段单独展示；未勾选、未填写或失败的内容不会从结果清单消失。
- 扫描阶段会像 0.8.x 一样先点击安全的“添加”按钮补齐空白记录，再读取招聘站点生成的真实字段模板；这些空白记录可能需要用户最后删除，但不会保存或提交。
- 已确认方案只授权其中的内容；动态出现但未预览的字段、新模板差异、其他经历的默认值，都不会被猜测填入。

## 记录决策

| 页面情况 | 处理 |
| --- | --- |
| 对应经历已存在、内容正确 | 内部检查后保留，不重复写入 |
| 同一条经历、正文旧版或部分事实错误 | 复核后列入原记录的更新方案；不因正文不同新增 |
| 已有空白记录 | 优先复用 |
| 简历中有、页面没有 | 基于现有模板新增并填写 |
| 同名经历、多个候选记录 | 用时间、学历、专业、职位进一步区分 |
| 仍不能确认身份或存在冲突 | 保留旧记录，另行补齐本方向的明确经历，并提示最后删查 |
| 页面多出的旧经历 | 保留并提示；不自动删除 |
| 区块/添加按钮/模板不存在 | 明确报告尚未填写，不假装覆盖完整简历 |
| 添加后没有新字段或新增记录带着别的经历内容 | 停止该项或整条记录，展示异常 |

## 内部检查

1. 从 DOM 读取标签、原始字段名、区块、记录序号、说明、相邻标签、控件类型、选项与字数限制。
2. 本地规则生成候选，按记录身份整理已有、空白、缺少和冲突项。正文不作为经历身份条件。
3. 千问独立解释全部可处理字段；该轮不展示规则候选路径，减少被规则误判带偏的风险。
4. 独立复核提示词检查前一轮解释和规则候选，并可修正冲突。使用同一个配置模型，不声称是两个独立模型。
5. 本地代码再次校验事实路径、记录归属、明确标签、选项、长度、生成文案引用与数字。不允许把本人信息写成紧急联系人，也不允许将实习事实挪成独立全职工作。
6. 千问超时、返回格式异常或漏回某个字段时，简历主库中的高置信精确映射会再次经过上述本地约束并自动准备；未知、冲突、截断和选项不匹配仍保持待确认。
7. 确认后添加缺少记录，按新字段的区块、序号、标签、控件类型重新绑定。已有值自预览后被修改、经历身份改变或页面跳转，会停止相关写入。
8. 每项写入后检查，并在整批结束后再回读，检测后续控件重置前面字段的情况。学校等远程搜索下拉框会等待结果，月份控件会按月精度提交。

## 验证与边界

- Node.js 语法检查与 99 项自动化回归测试；真实接口另以两个合成字段验证了一次千问调用和最终方案返回。
- 完整流程测试包含：生成尚未新增的教育字段方案 → 一次确认后新增并填写 → 回读核对 → 再扫描不重复新增。
- 保留原有字段、方向、日期、禁止自动提交等回归测试，新增模型复核、记录决策、版本兼容、未完成项保留等测试。
- 已在用户现有 Chrome 中验证字节与腾讯的空区块新增：字节新增一条空项目，腾讯分别新增一条空实习和空项目，均未填写、保存或提交。真实接口小样本耗时约 34 秒；侧栏会显示阶段与秒数，单次模型请求总预算为 25 秒，整次分析等待上限为 60 秒。
- 模型缺少密钥、网络失败、结果异常或漏回字段时，仅将通过本地严格校验的精确主库映射自动准备；其余候选保持待确认。
- 不支持 iframe/Shadow DOM 内部表单，也无法阻止网站自身的输入自动保存；不点击保存、下一步、提交按钮。

0.9.4 为侧栏界面更新：待补充项按经历分组，点开编辑；技术原因、统计详情与工具默认收起。确认按钮固定在底部，仅填入有内容的勾选项。本次本地合成数据预览验证了展开、编辑、计数和执行后剩余项保留。

重新加载扩展并重新打开侧栏即可使用新界面。本地服务保持 0.9.3，无需为本次界面更新重启服务。
